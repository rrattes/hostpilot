[project]
name = "webpanel-agent"
version = "0.1.0"
description = "Mock local agent for HostPilot."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
